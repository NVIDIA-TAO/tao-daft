{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "$id": "https://developer.nvidia.com/tao-daft/schemas/metropolis-v3.0/bcq.schema.json",
  "title": "Event Verification Task Annotation",
  "description": "Event verification task annotation for videos. Each item asks a yes/no question about whether a described event occurs in a video identified by video_id.",
  "type": "object",
  "required": [
    "version",
    "metadata",
    "items"
  ],
  "additionalProperties": false,
  "properties": {
    "version": {
      "type": "string",
      "const": "metropolis-v3.0",
      "description": "Schema version - must be '3.0' for this schema"
    },
    "items": {
      "type": "array",
      "description": "Array of verification items, one per event to verify",
      "minItems": 1,
      "items": {
        "type": "object",
        "required": [
          "question",
          "answer"
        ],
        "additionalProperties": false,
        "properties": {
          "video_id": {
            "type": "string",
            "description": "Reference to video ID (matches directory name in contextual/)"
          },
          "question": {
            "type": "string",
            "description": "Yes/no question about whether a specific event occurs in the video"
          },
          "answer": {
            "type": "string",
            "enum": [
              "Yes",
              "No"
            ],
            "description": "Answer: 'Yes' if the event occurs, 'No' otherwise"
          },
          "reasoning": {
            "type": "string",
            "description": "Optional step-by-step reasoning trace leading to the answer"
          },
          "image_id": {
            "type": "string",
            "description": "Reference to image_id in image_*.json",
            "minLength": 1
          }
        },
        "oneOf": [
          {
            "required": [
              "video_id"
            ]
          },
          {
            "required": [
              "image_id"
            ]
          }
        ]
      }
    },
    "metadata": {
      "type": "object",
      "description": "File metadata for identification and cataloging",
      "required": [
        "type"
      ],
      "properties": {
        "type": {
          "type": "string",
          "description": "Task type identifier (must be 'bcq')",
          "const": "bcq"
        },
        "date": {
          "type": "string",
          "description": "Creation date in ISO 8601 format (YYYY-MM-DD)",
          "format": "date"
        },
        "description": {
          "type": "string",
          "description": "Human-readable description of this annotation file"
        },
        "license": {
          "type": "string",
          "description": "License under which this annotation is distributed"
        },
        "tags": {
          "type": "array",
          "description": "List of tags for categorization and search",
          "items": {
            "type": "string"
          }
        }
      },
      "additionalProperties": false
    }
  }
}
